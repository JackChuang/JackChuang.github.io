source "https://rubygems.org"

# Matches the exact Jekyll + plugin versions GitHub Pages runs, so what you
# see with `bundle exec jekyll serve` is what GitHub will publish.
gem "github-pages", group: :jekyll_plugins
gem "webrick", "~> 1.8"   # required on Ruby 3.0+
